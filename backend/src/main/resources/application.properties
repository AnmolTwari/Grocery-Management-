# Grocery Shop Management System - Spring Boot backend
spring.application.name=grocery-backend

# Local development defaults to the "dev" profile.
# Override for deployments with SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=dev

# Common JPA settings
spring.jpa.open-in-view=false