# Copy this file to .env and fill in your own values.
# .env is git-ignored and must never be committed.

DB_URL=jdbc:postgresql://localhost:5432/grocerydb?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=change-me
